mod variables;
mod functions;
mod datatypes;

fn main() {
    // main function is the entry point into the program.

    // `Statement` are instructions that perform some action and do not return a value.
    // `Expressions` evaluate to a resultant value. Let’s look at some examples.

    // Rust is an expression-based language.

    /*
        Conventions : 
        - Rust code uses snake case as the conventional style for function and variable names.
        - Naming convention for constants is to use all uppercase with underscore between words
    */ 

    println!("Hello, world!");
    // variables::intro();
    // functions::intro();
    // datatypes::intro();
}
